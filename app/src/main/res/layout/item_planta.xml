<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="3dp"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- LINHA SUPERIOR: ícone + nomes -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Ícone da planta -->
            <TextView
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/bg_icon_planta"
                android:gravity="center"
                android:text="🌿"
                android:textSize="22sp" />

            <!-- Nomes -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:paddingStart="12dp">

                <TextView
                    android:id="@+id/txtNomePlantio"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Nome do Plantio"
                    android:textColor="#1B1B1B"
                    android:textSize="16sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/txtNomeEspecie"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:text="Nome da Espécie"
                    android:textColor="#6D8C6F"
                    android:textSize="13sp"
                    android:fontFamily="sans-serif-light" />

            </LinearLayout>

        </LinearLayout>

        <!-- DIVISOR -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="14dp"
            android:layout_marginBottom="12dp"
            android:background="#E8EDE8" />

        <!-- LINHA INFERIOR: 3 botões de ação -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end">

            <!-- Botão Informações -->
            <TextView
                android:id="@+id/btnInfo"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="@drawable/bg_btn_acao"
                android:gravity="center"
                android:text="ℹ"
                android:textColor="#2E7D32"
                android:textSize="16sp"
                android:layout_marginEnd="8dp" />

            <!-- Botão Editar -->
            <TextView
                android:id="@+id/btnEditar"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="@drawable/bg_btn_acao"
                android:gravity="center"
                android:text="✏"
                android:textColor="#2E7D32"
                android:textSize="16sp"
                android:layout_marginEnd="8dp" />

            <!-- Botão Excluir -->
            <TextView
                android:id="@+id/btnExcluir"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="@drawable/bg_btn_acao_vermelho"
                android:gravity="center"
                android:text="🗑"
                android:textColor="#C62828"
                android:textSize="16sp" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>